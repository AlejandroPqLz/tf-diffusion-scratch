# # Diffmodel.py # alg1
 
# class DiffModel(model)

# def train (training, sampling, plot_samples)

# def sampling (plot_samples) Sampler

# def save

# MODEL_PATH = "../models/XGBoost_model_v1"

# !rm -r {MODEL_PATH}

# mlflow.sklearn.save_model(model, MODEL_PATH)

# model = build_model()
# diffusion_model = DiffusionModel(model)
